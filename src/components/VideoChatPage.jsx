import React from 'react'
import { useParams } from 'react-router-dom'

const VideoChatPage = () => {
    const {id} = useParams();
  return (
    <div>
      VideoChatPage
    </div>
  )
}

export default VideoChatPage
